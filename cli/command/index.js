const build = require('./build');
const create = require('./create');
const dev = require('./dev');
const serve = require('./serve');
const prod = require('./prod');

module.exports = { build, prod, create, dev, serve }